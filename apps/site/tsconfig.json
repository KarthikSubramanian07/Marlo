{
  "//": "The site is generated by a plain .mjs script, so there is no source to compile. This project exists only so the no-theatre tests are typechecked and linted: an unchecked test can assert something impossible and pass by accident.",
  "extends": "../../tsconfig.spec-base.json",
  "compilerOptions": {
    "outDir": "../../node_modules/.cache/tsc-spec/site",
    "tsBuildInfoFile": "../../node_modules/.cache/tsc-spec/site/.tsbuildinfo",
    "rootDir": "./test"
  },
  "include": ["test/**/*.ts"]
}
