# Reproducibility. A lockfile that can be ignored is not a lockfile.
frozen-lockfile=false
engine-strict=true
# Adapters must not rely on transitive resolution. If a package uses it, it declares it.
auto-install-peers=false
strict-peer-dependencies=false
# Explicit is better. Approve build scripts one at a time.
enable-pre-post-scripts=true
